# legacy-peer-deps allows npm to resolve peer dependency mismatches
# (e.g. @vitejs/plugin-vue peer spec lagging behind a vite major version).
# This project is not published to npm — the setting is safe for a local build tool.
legacy-peer-deps=true
