# EDMD requirements.txt
#
# IMPORTANT: Not all dependencies should be installed via pip.
# Run install.sh instead of pip install -r requirements.txt directly.
#
# ── System package manager — Linux only (NOT pip) ────────────────────────────
#
#   psutil and PyGObject have C extensions that require system libraries.
#   Install via your distro package manager:
#
#   psutil (required on all platforms for session management):
#     Arch:          sudo pacman -S python-psutil
#     Debian/Ubuntu: sudo apt install python3-psutil
#     Fedora:        sudo dnf install python3-psutil
#
# ── pip (all platforms) ───────────────────────────────────────────────────────
# Textual is now a core dependency and no longer needs to be installed separately.
discord-webhook>=1.3.0
cryptography>=41.0.0
textual>=0.47.0
# ── stdlib (no install needed) ────────────────────────────────────────────────
#   tomllib, argparse, json, pathlib, re, queue, threading, datetime

# ── Electron bridge (required only for --mode electron) ──────────────────────
# ── Electron GUI bridge (optional — only needed for --mode electron) ───────────
# websockets>=12.0
# Install with: pip install 'websockets>=12.0'
