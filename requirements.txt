# EDMD requirements.txt
#
# IMPORTANT: Not all dependencies should be installed via pip.
# Run install.sh (Linux) or install.bat (Windows) instead of pip install -r.
#
# ── System package manager (NOT pip) ──────────────────────────────────────────
#
#   These have C extensions requiring system libraries.
#   Install via your distro package manager:
#
#   Arch:          sudo pacman -S python-psutil python-gobject gtk4
#   Debian/Ubuntu: sudo apt install python3-psutil python3-gi gir1.2-gtk-4.0
#   Fedora:        sudo dnf install python3-psutil python3-gobject gtk4
#   Windows:       pip install psutil  (GTK4/PyGObject not supported on Windows)
#
#   psutil>=5.9.0        (auto-quit — process management)
#   PyGObject>=3.42.0    (GTK4 GUI — Linux only, optional)
#     Modules used: Gtk, GLib, Gdk, Pango
#     Pango is bundled with GTK4 — no separate install needed.
#
# ── pip ───────────────────────────────────────────────────────────────────────
#
discord-webhook>=1.3.0
cryptography>=41.0.0
#
# ── stdlib (no install needed) ────────────────────────────────────────────────
#   tomllib, argparse, json, pathlib, re, queue, threading, datetime
